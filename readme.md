# PyDocSearcher

1. Indexes the file in the directory, stores the indexers, uses that to look up specific keyword. 
    (as of now, only Txt files)
2. Used from the terminal-CLI


# TODO

1. Other file type parser
2. Serve it in a UI
3. Use NLP or integrate with embeddings for faster retrieval and similar searchers
